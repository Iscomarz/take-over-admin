export class CFormaPago{
    constructor({idFormaPago, nombre, descripcion, activo}){
        this.idFormaPago = idFormaPago,
        this.nombre = nombre,
        this.descripcion = descripcion,
        this.activo = activo
    }
}